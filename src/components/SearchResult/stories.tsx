import { Story, Meta } from '@storybook/react/types-6-0'
import SearchResult from '.'

export default {
  title: 'SearchResult',
  component: SearchResult
} as Meta

export const Default: Story = () => <SearchResult />
